import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'extract-text',
  name: 'Extract Text',
  description: 'Pull text out of a PDF, preserving line and paragraph structure. Runs in your browser.',
  category: 'Convert',
}
