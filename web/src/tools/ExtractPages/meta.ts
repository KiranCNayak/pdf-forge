import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'extract-pages',
  name: 'Extract Pages',
  description: 'Pull a selection of pages out of a PDF into one new document.',
  category: 'Organize',
}
