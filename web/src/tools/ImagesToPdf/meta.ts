import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'images-to-pdf',
  name: 'Images to PDF',
  description: 'Combine JPEG, PNG, TIFF or WebP images into one PDF, one page per image.',
  category: 'Convert',
}
