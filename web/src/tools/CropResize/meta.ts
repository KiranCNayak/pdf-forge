import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'crop-resize-pdf',
  name: 'Crop & Resize',
  description: 'Trim margins off the visible area, or scale the whole page to a new size.',
  category: 'Organize',
}
