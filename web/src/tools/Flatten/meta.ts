import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'flatten-pdf',
  name: 'Flatten',
  description: 'Bake filled form fields and annotations into static page content — the values stay, the fields don’t.',
  category: 'Organize',
}
