import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'sign-pdf',
  name: 'Sign',
  description: 'Draw a signature and stamp it onto a page — nothing else in the document is touched.',
  category: 'Annotate',
}
