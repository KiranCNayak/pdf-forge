import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'merge',
  name: 'Merge PDFs',
  description: 'Combine PDFs in any order. Everything runs on this device — nothing is uploaded.',
  category: 'Organize',
}
