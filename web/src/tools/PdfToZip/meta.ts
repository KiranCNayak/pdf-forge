import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'pdf-to-zip',
  name: 'PDF to ZIP',
  description: 'Render every page to an image and download them as one ZIP. Runs in your browser.',
  category: 'Convert',
}
