import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'invert-pdf',
  name: 'Invert Colours',
  description: 'Flip every page to a dark background with light text — a photo-negative, not a colour-aware edit.',
  category: 'Convert',
}
