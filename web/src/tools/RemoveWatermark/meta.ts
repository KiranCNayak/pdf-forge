import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'remove-watermark',
  name: 'Remove Watermark',
  description: 'Strip a watermark someone else stamped onto a PDF — all pages, or a selection.',
  category: 'Annotate',
}
