import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'redact-pdf',
  name: 'Redact',
  description: 'Permanently black out sensitive content — the page is rebuilt as an image, so nothing under the box survives.',
  category: 'Security',
}
