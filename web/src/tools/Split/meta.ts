import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'split-pdf',
  name: 'Split PDF',
  description: 'Cut one PDF into several — every page, fixed-size chunks, or custom ranges.',
  category: 'Organize',
}
