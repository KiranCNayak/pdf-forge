import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'rotate-pdf',
  name: 'Rotate PDF',
  description: 'Fix sideways or upside-down pages — rotate the whole document or a selection.',
  category: 'Organize',
}
