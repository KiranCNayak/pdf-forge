import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'fingerprint-pdf',
  name: 'Fingerprint',
  description: 'Stamp a faint, unique per-copy code onto every page — trace which recipient a leaked copy came from.',
  category: 'Security',
}
