import type { ToolMeta } from '../registry'

export const meta: ToolMeta = {
  route: 'remove-password',
  name: 'Remove Password',
  description: 'Decrypt a PDF you can already open, producing an unprotected copy.',
  category: 'Security',
}
